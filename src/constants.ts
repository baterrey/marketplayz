export enum Signals {
  hot = 'hot',
  cold = 'cold',
  normal = 'normal',
}

export enum Devices {
  switch = 'switch',
  airConditioner = 'airConditioner',
  waterHeater = 'waterHeater',
}

export const hourWhenOwnerComes: number = 19;
